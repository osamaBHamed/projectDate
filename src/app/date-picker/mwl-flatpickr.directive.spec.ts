import { MwlFlatpickrDirective } from './mwl-flatpickr.directive';

describe('MwlFlatpickrDirective', () => {
  it('should create an instance', () => {
    const directive = new MwlFlatpickrDirective();
    expect(directive).toBeTruthy();
  });
});
